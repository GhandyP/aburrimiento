
name: analizador_aburrimiento
description: Aplicación Flutter para análisis de aburrimiento grupal con ML

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # HTTP client para conexión con backend Python
  http: ^1.1.0

  # Para ML on-device (opcional)
  tflite_flutter: ^0.10.0

  # Para análisis de sentimientos (opcional)
  google_ml_kit: ^0.16.0

  # State management
  provider: ^6.0.5

  # JSON serialization
  json_annotation: ^4.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  json_serializable: ^6.7.1
  build_runner: ^2.4.6

flutter:
  uses-material-design: true
