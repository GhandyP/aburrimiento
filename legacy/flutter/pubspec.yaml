name: aburrimiento_minimal
description: Minimal boredom analyzer Flutter UI.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  http: ^1.2.2

flutter:
  uses-material-design: true
